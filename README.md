# Website Status Checker
A simple Python script to check if a website is up and running. 

## Features
- Checks HTTP status codes using the `requests` library.
- Handles connection errors.

## How to run
1. Install requirements: `pip install requests`
2. Run the script: `python3 website_check.py`

